use std::collections::HashMap;
use std::io::BufReader;
use std::io::Lines;
use std::io::Result;
use std::path::Path;
use std::io::BufRead;
use std::fs::File;

// CHALLENGE USE include_bytes

fn main() {

}

